<?php


// Class for the user_submission table
class UserSubmission{
    
    /*
    public static function getTopXUsersDeliveredByAssignment($db, $assignmentID, $numberOfStudents) {
        $sql = "SELECT * FROM user_submissions WHERE assignment_id = ?";  
        $stmt = $db->prepare($sql);
        $param = array($assignmentID);
        $stmt->execute($param);
        
        return $stmt->fetch(PDO::FETCH_ASSOC);
    }
    */
}
